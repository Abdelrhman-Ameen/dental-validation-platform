# Firebase Deploy Assets

The canonical deploy files live at the project root so `firebase deploy` can run from `WebSite`:

- `firebase.json`
- `firestore.rules`
- `storage.rules`
- `firestore.indexes.json`

This folder is reserved for Firebase notes, exported emulator data, and future environment-specific rule variants.
